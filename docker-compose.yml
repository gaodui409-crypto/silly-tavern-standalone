version: "3.8"

services:
  taverndb:
    build: .
    ports:
      - "7892:7892"
    environment:
      - PORT=7892
      - TEST_API_URL=${TEST_API_URL:-}
      - TEST_API_KEY=${TEST_API_KEY:-}
      - TEST_MODEL=${TEST_MODEL:-moonshotai/kimi-k2.5}
    restart: unless-stopped
